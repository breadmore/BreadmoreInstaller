# Breadmore Installer

Tiny public bootstrap package. It stores a GitHub PAT in local EditorPrefs, seeds the OS git credential helper, and installs the private `com.breadmore.core` package from `breadmore/BreadmoreCore`.

This package intentionally does not include Breadmore Core runtime utilities, adapter templates, or the full package manager implementation.
